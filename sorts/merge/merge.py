# https://en.wikipedia.org/wiki/Merge_sort
"""
Merge sort divides and conquers.
First divide the set into its smallest components.
Then (conquer) start combining the components 
"""

def sort(toSort):
    # no need to sort if the list is 0 or 1 elements
    if len(toSort) <= 1:
        return toSort
        
    # divide
    if len(toSort) > 1:
        # gonna have to round up
        # https://stackoverflow.com/questions/2356501/how-do-you-round-up-a-number-in-python
        halfWay = 
        
    

if __name__ == "__main__":
    print "import and call sort() rather than running this script"